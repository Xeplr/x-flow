# @xeplr/ui-workflow — the standalone dev app's ports and upstreams.
# Copy to .env. Read by vite.config.js (loadEnv), so no port is written in code.

UI_PORT=19120
AUTH_URL=http://localhost:19101
API_URL=http://localhost:19122
